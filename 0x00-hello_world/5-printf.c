#include <stdio.h>

/**
 * main - this is a must in every code
 *
 * Return: should most at times return 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
